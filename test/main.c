#include <limits.h>
#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    _printf("We built this city on %s, with %d units, of type %c, in %i hour(s) at 80%%", "Rock & Roll", 2, 'U', 1);
    return (0);
}
